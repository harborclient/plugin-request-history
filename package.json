{
  "name": "harborclient-plugin-recent-requests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/renderer.tsx --bundle --outfile=dist/renderer.js --format=esm --platform=neutral --jsx=automatic --jsx-import-source=@harborclient/plugin-api --external:react --external:react-dom",
    "verify": "node scripts/verify.mjs",
    "dev": "esbuild src/renderer.tsx --bundle --outfile=dist/renderer.js --format=esm --platform=neutral --jsx=automatic --jsx-import-source=@harborclient/plugin-api --external:react --external:react-dom --watch"
  },
  "devDependencies": {
    "@harborclient/plugin-api": "^0.3.2",
    "@types/react": "^19.1.8",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3"
  }
}
